spring.application.name=CodeCampus

# --- C?U HÌNH DB THÔNG MINH ---
# N?u có bi?n môi tr??ng (trên Render) thì dùng, không thì dùng m?c ??nh (Local SQL Server)
spring.datasource.url=${DB_URL:jdbc:sqlserver://localhost:1433;databaseName=codecampus_db;encrypt=true;trustServerCertificate=true;sendStringParametersAsUnicode=true;}
spring.datasource.username=${DB_USERNAME:sa}
spring.datasource.password=${DB_PASSWORD:123}
spring.datasource.driver-class-name=${DB_DRIVER_CLASS_NAME:com.microsoft.sqlserver.jdbc.SQLServerDriver}
spring.jpa.database-platform=${DB_PLATFORM:org.hibernate.dialect.SQLServerDialect}

# --- C?U HÌNH CHUNG ---
spring.jpa.show-sql=true
# ?? update ?? khi lên server nó t? t?o b?ng trong MySQL
spring.jpa.hibernate.ddl-auto=update
server.port=${PORT:8080}

# --- CÁC API KEY (S? ?i?n trên Render) ---
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:adinh479@gmail.com}
spring.mail.password=${MAIL_PASSWORD:pass_local_neu_co}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET}
spring.security.oauth2.client.registration.google.scope=email,profile

google.api.key=${GOOGLE_AI_KEY}

payos.client-id=${PAYOS_CLIENT_ID}
payos.api-key=${PAYOS_API_KEY}
payos.checksum-key=${PAYOS_CHECKSUM_KEY}